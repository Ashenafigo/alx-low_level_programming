/**
 * main - Entry point
 *
 * puts writes or displays what's put into it as an argument
 *
 * Return: Always 0 (Success)
 */

#include <stdio.h>

int main()
{
	char message[60] = "\"Programming is like building a multilingual puzzle";
	
	puts(message);

	return (0);
}
